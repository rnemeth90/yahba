apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yahba.fullname" . }}-test-connection"
  labels:
    {{- include "yahba.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yahba.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
